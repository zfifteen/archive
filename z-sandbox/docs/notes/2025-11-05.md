# Daily Notes — 2025-11-05

**Best result:** combined with rel_distance=0.000485 (0.0485%)

## What worked:
- **Comb step refinement:** Reducing step from 0.001 → 0.0001 yielded **37% improvement** (0.0772% → 0.0485%)
- **Combined pipeline:** No degradation vs. components (monotonicity preserved)
- **Embedding offset=0.0:** Default was optimal (no bias adjustment needed for fractional comb)

## What didn't work:
- **Embedding offset sweep:** All values {-0.002, -0.001, -0.0005} produced identical results (0.0772%)
  - Confirms embedding_offset is **incompatible** with fractional comb mode (doesn't affect log_center)
  - Bias corrections only work when use_fractional_comb=False
- **Comb step plateau:** Steps 0.001 and 0.0005 both gave 0.0772% (no improvement until 0.0001)

## Key findings:
- **Fractional comb center:** Uses log_N/2 directly, ignoring bias model and embedding_offset
- **Best configuration:** Pure comb (no bias) with very fine step (0.0001)
- **Absolute distance:** Still ~6.5e304, far from ±1000 target (need ~10^302× improvement)
- **Relative improvement:** 0.0772% → 0.0485% (37% reduction, exceeds 5% acceptance threshold)

## Next knobs to try:
1. **Even finer comb steps:** Try {5e-5, 1e-5} to see if trend continues
2. **Log-space bias for comb:** Modify greens_function_factorization.py line 465 to apply bias to log_center
3. **Quadratic crest interpolation:** Implement parabolic vertex fit on adjacent peaks
4. **Wider comb range:** Try comb_range={2, 5, 10} with step=0.0001
5. **k-value sweep:** Try k ∈ {0.20, 0.25, 0.30} with best comb step

## Acceptance criteria:
✅ **SOFT ACCEPT:** 37% improvement exceeds 2-5% threshold with reduced abs_distance
