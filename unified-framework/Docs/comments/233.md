### Update: Empirical Implementation Plan and Alignment for Z_5D Integration

#### Empirical Alignment and Implementation Summary

This comment details the empirically-driven implementation plan for the Z_5D predictor, ensuring invariant-respecting, reproducible integration into the unified codebase. The approach is modular, vectorized, and rigorously validated, in alignment with the Z Framework’s universal normalization principles.

---

#### 1. **Module Placement & Structure**
- **Directory:** Create `src/z_framework/discrete/` with `__init__.py` for import hygiene and future extensibility.
- **Core Implementation:** Place all vectorized Z_5D logic in `src/z_framework/discrete/z5d_predictor.py`.

#### 2. **Predictor Implementation**

- **Core Functions:**  
  - `base_pnt_prime(k)`: Vectorized Prime Number Theorem estimator (handles scalar/array).
  - `d_term(k)`: Dilation term as specified.
  - `e_term(k)`: Curvature term as specified.
  - `z5d_prime(k, c=-0.00247, k_star=0.04449)`: Returns the Z_5D estimate for nth prime.
  - All functions use numpy for masking and vectorization.

- **Edge Cases:**  
  - For \( k < 2 \), return 0 (masked).
  - For \( p_{PNT}(k) \leq 1 \), \( d(k) = 0 \); for \( p_{PNT}(k) = 0 \), \( e(k) = 0 \).
  - Use float64 for standard precision, with optional mpmath for ultra-large \( k \).

#### 3. **Calibration & Utilities**
- **Fitting:**  
  - Private fitting functions for least-squares calibration of \( c \) and \( k^* \) using `scipy.optimize.curve_fit`.
  - If calibration utilities grow, migrate to `src/utils/calibration.py`.

#### 4. **Pipeline Integration**
- **Integration:**  
  - Explicit import into any existing prime predictor selectors (if-else or enum).
  - Integration points documented via code comments and docstrings.
  - No abstract interface assumed; manual wiring as required.

#### 5. **Testing & Benchmarks**
- **Tests:**  
  - Add `tests/test_z5d_predictor.py` with deterministic tests (hardcoded \( k \) and true primes).
  - Supplement with randomized tests using `sympy.ntheory.prime` for ground truth.
- **Benchmarking:**  
  - Assert error <0.01% for \( k = 10^6 \).
  - Generate error histograms on the fly; commit static plots to `docs/` for empirical tracking.

#### 6. **Documentation**
- **Docstrings:**  
  - All public functions use NumPy-style docstrings, including algebraic expressions and usage examples.
- **Readme/docs:**  
  - README.md updated with summary, empirical tables/plots, and core equations.
  - Detailed empirical results in `docs/empirical_results.md` if needed.

#### 7. **Dependencies**
- **Requirements:**  
  - Update both `requirements.txt` and `pyproject.toml` for numpy, scipy, sympy.
  - Optional dependencies (mpmath) listed as extras in `pyproject.toml`.

#### 8. **Logging**
- **Numerical Issues:**  
  - Use Python logging at WARNING level for overflow/precision warnings, with invariant-safe fallbacks.

---

### Acceptance Criteria (Reiterated)
- Vectorized Z_5D matches empirical benchmarks (e.g., \( p_{Z_5D}(10^6) \approx 15485845.91 \), error <0.0001%).
- Tests pass for scalar and array inputs; edge cases handled.
- Documentation and empirical validation are reproducible and clear.
- Integration points and utilities are modular and extensible.

---

**No further alignment or clarification is required at this stage. Proceeding with a minimal, empirically validated core in the first PR, with follow-up enhancements (calibration utilities, hybrid sieving, etc.) staged for subsequent iterations.**

---